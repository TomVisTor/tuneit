<div class="banner">
  <div class="container">
    <h1>Tune It</h1>
    <p>Louer vos instruments de musique de particulier à particulier</p>
  </div>
</div>

<div class="container">
  <h2>Derniers instruments postés</h2>

  <div class="row">
    <% @instruments.first(3).each do |instrument| %>
    <div class="col-sm-12 col-md-4">
      <%= link_to instrument_path(instrument) do %>
        <div class="card">
          <%= cl_image_tag instrument.photo.key, class: "card-img-top" %>
          <h3><%= instrument.name %></h3>
          <p>Nantes | 16€/day</p>
        </div>
      <% end %>
    </div>
  <% end %>

  <div class="container-btn">
    <%= link_to "Voir plus", instruments_path, class: "button"  %>
  </div>
</div>
